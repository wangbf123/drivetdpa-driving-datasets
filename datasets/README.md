# 数据集发布目录

本仓库正式数据全部来源于 CARLA。大文件存放在对象存储中，每个子目录说明一种数据集，并链接对应 manifest。

- `carla/`：传感器、Autoware、BEV、Talk2BEV 和 DriveTDPA 多模态数据。
- `preference/`：同一 CARLA 帧多候选轨迹及偏好选择数据。
- `closed_loop/`：控制指令到下一帧反馈的闭环数据。
